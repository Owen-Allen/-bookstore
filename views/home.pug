script(src="client.js") 
head
    p This is the test for the home page
body
    p Book List:
        p Search:
            input#searchCondition(type="textbox")
            button#searchButton(type="button" onclick="search()") Search for Users
    ul
        for row in rows
            li= `Title: ${row.title}, ISBN: ${row.isbn}`
            a(href= `https://boppa-bat-bookstore.herokuapp.com/nameSearch/${row.title}`) Link to specific book page
